<view class="switch-picker">
  <picker class="selector" mode="selector" bind:change="change" range="{{data}}" range-key="label" value="{{value}}">
    <view class="picker">{{data[value].label}}</view>
  </picker>
  <view class="wp-item">
    {{data[value].content}}
  </view>
</view>