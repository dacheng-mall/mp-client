<scroll-view scroll-y class="instWrap">
  <block wx:for="{{list}}" wx:key="{{item.id}}">
    <view class="instItem" data-id="{{item.id}}" data-name="{{item.name}}" catchtap="next">
      {{item.name}}
      <icon-x type="right" />
    </view>
  </block>
</scroll-view>